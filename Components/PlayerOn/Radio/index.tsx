import React from "react";
import { Background } from "../common";

interface Props {}

export const Radio = (props: Props) => {
  return (
    <Background>
      <h1>Radio Content right here</h1>
    </Background>
  );
};
