import React from "react";
import { Background } from "../common";

interface Props {}

export const Genres = (props: Props) => {
  return (
    <Background>
      <h1>Genres Content right here</h1>
    </Background>
  );
};
