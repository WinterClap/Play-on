import React from "react";
import { Background } from "../common";

interface Props {}

export const Explore = (props: Props) => {
  return (
    <Background>
      <h1>Explore Content right here</h1>
    </Background>
  );
};
